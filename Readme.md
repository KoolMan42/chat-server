# Mock websocket chat server
The server sends out JSON to the clients that consume it.


# Chat client
The client is a Svelte app that consumes a websocket and displays chat messages. 

I really like Svelte